name: benjlu

on:
  push:
    branches: [ main ]

jobs:
  job1:
    name: build
    runs-on: self-hosted
    steps:
      - name: step one
        uses: actions/checkout@v2
      - name: step two
        run: npm i
      - name: step three
        run: composer install
      - name: step four
        run: pwd